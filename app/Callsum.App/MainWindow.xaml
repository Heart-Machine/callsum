<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Callsum.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Состояние связи с OBS: видно сразу, без раскрытия журнала. -->
        <TextBlock
            x:Name="ObsStatus"
            Grid.Row="0"
            Text="OBS: подключаюсь…"
            Opacity="0.6" />

        <Button
            x:Name="RecordButton"
            Grid.Row="1"
            HorizontalAlignment="Stretch"
            Height="56"
            FontSize="17"
            FontWeight="SemiBold"
            IsEnabled="False"
            Content="● Начать запись"
            Click="OnRecordClick" />

        <TextBlock
            x:Name="Timer"
            Grid.Row="2"
            HorizontalAlignment="Center"
            FontSize="30"
            FontFamily="Consolas"
            Text="00:00:00" />

        <StackPanel Grid.Row="3" Spacing="6">
            <TextBlock
                x:Name="Stage"
                HorizontalAlignment="Center"
                Text="Готов к записи" />

            <!-- Полоса прячется, когда обработки нет: пустая шкала сбивает с толку. -->
            <ProgressBar
                x:Name="Progress"
                Visibility="Collapsed"
                Minimum="0"
                Maximum="1" />
        </StackPanel>

        <Border
            Grid.Row="4"
            BorderThickness="1"
            CornerRadius="4"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <ScrollViewer>
                <TextBlock
                    x:Name="Log"
                    Padding="10"
                    FontFamily="Consolas"
                    FontSize="12"
                    TextWrapping="Wrap"
                    IsTextSelectionEnabled="True" />
            </ScrollViewer>
        </Border>

        <TextBlock
            Grid.Row="5"
            Opacity="0.6"
            FontSize="12"
            Text="Запись ведёт OBS: приложение переключает его на свой профиль и возвращает прежний после остановки." />
    </Grid>
</Window>
